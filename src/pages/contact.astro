---
import MainLayout from '../layouts/MainLayout.astro';
import { translations } from '../i18n/translations';
const t = translations.fr;
---

<MainLayout title="Contact — Nal Consulting">

  <section class="contact-page">
    <div class="contact-page__bg" aria-hidden="true">
      <div class="cp-blob cp-blob--1"></div>
      <div class="cp-blob cp-blob--2"></div>
      <div class="cp-blob cp-blob--3"></div>
      <div class="cp-dots"></div>
    </div>

    <div class="container contact-inner">

      {/* En-tête avec word-reveal */}
      <div class="contact-top">
        <span class="eyebrow eyebrow--blanc reveal-fade" data-i18n="contact.eyebrow">— Contact</span>
        <h1 class="contact-titre">
          <span class="contact-titre__line titre-words" data-words={t.contact.heroLine1} data-i18n-words="contact.heroLine1"></span>
          <span class="contact-titre__line contact-titre__line--grad titre-words" data-words={t.contact.heroLine2} data-i18n-words="contact.heroLine2"></span>
        </h1>
        <p class="contact-desc reveal-fade" data-i18n="contact.heroSub">
          {t.contact.heroSub}
        </p>
      </div>

      {/* ═══ SECTION CALENDLY — Voie rapide ═══ */}
      <div class="calendly-hero reveal-fade">
        <div class="calendly-hero__bg" aria-hidden="true">
          <div class="calendly-hero__glow"></div>
          <div class="calendly-hero__grid"></div>
        </div>
        <div class="calendly-hero__content">
          <div class="calendly-hero__left">
            <span class="calendly-hero__badge">
              <span class="calendly-hero__badge-pulse"></span>
              <span data-i18n="contact.calendly.badge">Le plus rapide</span>
            </span>
            <h2 class="calendly-hero__titre">
              <span data-i18n="contact.calendly.line1">Réservez 30 minutes</span><br/>
              <span class="calendly-hero__titre-grad" data-i18n="contact.calendly.line2">en direct avec moi.</span>
            </h2>
            <p class="calendly-hero__desc" data-i18n="contact.calendly.desc">
              Un échange visio sans engagement pour comprendre votre besoin et identifier les bons leviers. Choisissez votre créneau, je m'occupe du reste.
            </p>
            <div class="calendly-hero__features">
              <span class="calendly-hero__feat">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                <span data-i18n="contact.calendly.feat1">30 minutes</span>
              </span>
              <span class="calendly-hero__feat">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                <span data-i18n="contact.calendly.feat2">Visio Google Meet</span>
              </span>
              <span class="calendly-hero__feat">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                <span data-i18n="contact.calendly.feat3">Sans engagement</span>
              </span>
            </div>
            <a href="https://calendly.com/muniernael/30min" target="_blank" rel="noopener noreferrer" class="calendly-hero__cta">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <rect x="3" y="4" width="18" height="18" rx="2"/>
                <line x1="16" y1="2" x2="16" y2="6"/>
                <line x1="8" y1="2" x2="8" y2="6"/>
                <line x1="3" y1="10" x2="21" y2="10"/>
              </svg>
              <span data-i18n="contact.calendly.cta">Réserver mon créneau</span>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <line x1="5" y1="12" x2="19" y2="12"/>
                <polyline points="12 5 19 12 12 19"/>
              </svg>
            </a>
          </div>
          <div class="calendly-hero__right" aria-hidden="true">
            <div class="calendly-hero__visual">
              <div class="calendly-hero__visual-cal">
                <div class="calendly-hero__visual-head">
                  <span></span><span></span><span></span>
                </div>
                <div class="calendly-hero__visual-grid">
                  <div></div><div></div><div></div><div></div><div></div><div></div><div></div>
                  <div></div><div></div><div></div><div></div><div></div><div></div><div></div>
                  <div></div><div></div><div class="active"></div><div></div><div></div><div></div><div></div>
                  <div></div><div></div><div></div><div></div><div></div><div></div><div></div>
                </div>
                <div class="calendly-hero__visual-time">10:30 — 11:00</div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="contact-divider" aria-hidden="true">
        <span class="contact-divider__line"></span>
        <span class="contact-divider__text" data-i18n="contact.divider">ou par message</span>
        <span class="contact-divider__line"></span>
      </div>

      <div class="contact-grid">

        {/* Carte coordonnées premium */}
        <aside class="cp-card reveal-fade">
          <div class="cp-card__halo" aria-hidden="true"></div>

          <div class="cp-card__head">
            <span class="cp-card__eyebrow" data-i18n="contact.cardEyebrow">{t.contact.cardEyebrow}</span>
            <h2 class="cp-card__titre">
              <span class="titre-words" data-words={t.contact.cardLine1} data-i18n-words="contact.cardLine1"></span><br/>
              <span class="cp-card__titre-grad titre-words" data-words={t.contact.cardLine2} data-i18n-words="contact.cardLine2"></span>
            </h2>
          </div>

          <div class="cp-card__items">
            <a href="mailto:naelmunier22@gmail.com" class="cp-item cp-item--link" aria-label="Envoyer un email">
              <div class="cp-item__ico" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
                  <rect x="3" y="5" width="18" height="14" rx="2"/>
                  <path d="M3 7l9 6 9-6"/>
                </svg>
              </div>
              <div class="cp-item__txt">
                <span class="cp-item__label" data-i18n="contact.itemEmailLabel">Email</span>
                <span class="cp-item__val">naelmunier22@gmail.com</span>
              </div>
              <svg class="cp-item__arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <line x1="5" y1="12" x2="19" y2="12"/>
                <polyline points="12 5 19 12 12 19"/>
              </svg>
            </a>

            <a href="tel:+33621862854" class="cp-item cp-item--link" aria-label="Appeler">
              <div class="cp-item__ico" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
                </svg>
              </div>
              <div class="cp-item__txt">
                <span class="cp-item__label" data-i18n="contact.itemPhoneLabel">Téléphone</span>
                <span class="cp-item__val">06 21 86 28 54</span>
              </div>
              <svg class="cp-item__arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <line x1="5" y1="12" x2="19" y2="12"/>
                <polyline points="12 5 19 12 12 19"/>
              </svg>
            </a>

            <div class="cp-item cp-item--info">
              <div class="cp-item__ico cp-item__ico--alt" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="12" cy="12" r="10"/>
                  <polyline points="12 6 12 12 16 14"/>
                </svg>
              </div>
              <div class="cp-item__txt">
                <span class="cp-item__label" data-i18n="contact.itemAvailLabel">Disponibilité</span>
                <span class="cp-item__val" data-i18n="contact.itemAvailVal">{t.contact.itemAvailVal}</span>
              </div>
            </div>
          </div>

          <div class="cp-card__separator" aria-hidden="true"></div>

          <div class="cp-socials">
            <a href="https://linkedin.com" target="_blank" rel="noopener noreferrer" class="cp-social" aria-label="LinkedIn">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
              LinkedIn
            </a>
            <a href="https://github.com/naelmunier" target="_blank" rel="noopener noreferrer" class="cp-social" aria-label="GitHub">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M12 .297c-6.63 0-12 5.373-12 12 0 5.303 3.438 9.8 8.205 11.385.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61C4.422 18.07 3.633 17.7 3.633 17.7c-1.087-.744.084-.729.084-.729 1.205.084 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405 2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 22.092 24 17.592 24 12.297c0-6.627-5.373-12-12-12"/></svg>
              GitHub
            </a>
          </div>
        </aside>

        {/* Formulaire */}
        <div class="cp-form-wrap reveal-fade">
          <div class="cp-form-head">
            <span class="cp-card__eyebrow" data-i18n="contact.formEyebrow">{t.contact.formEyebrow}</span>
            <h2 class="cp-card__titre">
              <span class="titre-words" data-words={t.contact.formLine1} data-i18n-words="contact.formLine1"></span><br/>
              <span class="cp-card__titre-grad titre-words" data-words={t.contact.formLine2} data-i18n-words="contact.formLine2"></span>
            </h2>
          </div>

          <form
            class="cp-form"
            action="/api/send-form.php"
            method="POST"
            id="contactForm"
          >
            <input type="hidden" name="service" value="contact" />
            <div class="cp-form__row">
              <div class="form-group">
                <label for="nom" data-i18n="contact.formNomLabel">Nom</label>
                <input type="text" id="nom" name="nom" placeholder={t.contact.formNomPh} data-i18n-placeholder="contact.formNomPh" required />
              </div>
              <div class="form-group">
                <label for="email" data-i18n="contact.formEmailLabel">Email</label>
                <input type="email" id="email" name="email" placeholder={t.contact.formEmailPh} data-i18n-placeholder="contact.formEmailPh" required />
              </div>
            </div>
            <div class="form-group">
              <label for="message" data-i18n="contact.formMessageLabel">Message</label>
              <textarea id="message" name="message" rows={6} placeholder={t.contact.formMessagePh} data-i18n-placeholder="contact.formMessagePh" required></textarea>
            </div>
            <button type="submit" class="btn btn--primaire cp-submit" id="contactSubmit">
              <span data-i18n="contact.formSubmit">{t.contact.formSubmit}</span>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
            </button>
            <p class="cp-note" data-i18n="contact.formNote">{t.contact.formNote}</p>
          </form>
        </div>

      </div>
    </div>
  </section>

</MainLayout>

<script>
  // Form submit
  const form = document.getElementById('contactForm') as HTMLFormElement | null;
  const btn  = document.getElementById('contactSubmit') as HTMLButtonElement | null;
  form?.addEventListener('submit', () => {
    if (btn) btn.disabled = true;
  });

  // Word reveal
  function splitIntoWords(el: HTMLElement) {
    if (el.dataset.split === '1') return;
    const text = el.dataset.words ?? el.textContent ?? '';
    el.textContent = '';
    const words = text.split(/(\s+)/);
    words.forEach((w) => {
      if (/^\s+$/.test(w)) {
        el.appendChild(document.createTextNode(w));
      } else if (w.length) {
        const span = document.createElement('span');
        span.className = 'word';
        span.textContent = w;
        el.appendChild(span);
      }
    });
    el.dataset.split = '1';
  }

  const titres = document.querySelectorAll<HTMLElement>('.titre-words');
  titres.forEach(splitIntoWords);

  // Hero immédiat
  window.addEventListener('load', () => {
    const heroLines = document.querySelectorAll<HTMLElement>('.contact-titre__line');
    heroLines.forEach((line, lineIdx) => {
      const words = line.querySelectorAll<HTMLElement>('.word');
      words.forEach((w, i) => {
        setTimeout(() => w.classList.add('is-revealed'), 200 + lineIdx * 320 + i * 80);
      });
      (line as HTMLElement).dataset.revealed = '1';
    });
  });

  // Scroll observer pour autres titres
  const titresObs = new IntersectionObserver(
    (entries) => {
      entries.forEach((e) => {
        if (!e.isIntersecting) return;
        const el = e.target as HTMLElement;
        if (el.dataset.revealed === '1') return;
        el.dataset.revealed = '1';
        const words = el.querySelectorAll<HTMLElement>('.word');
        words.forEach((w, i) => {
          setTimeout(() => w.classList.add('is-revealed'), i * 70);
        });
        titresObs.unobserve(el);
      });
    },
    { threshold: 0.3, rootMargin: '0px 0px -40px 0px' }
  );
  titres.forEach((t) => {
    if (!t.classList.contains('contact-titre__line')) titresObs.observe(t);
  });

  // Reveal-fade
  const fadeEls = document.querySelectorAll<HTMLElement>('.reveal-fade');
  const fadeObs = new IntersectionObserver(
    (entries) => {
      entries.forEach((e) => {
        if (!e.isIntersecting) return;
        e.target.classList.add('is-revealed');
        fadeObs.unobserve(e.target);
      });
    },
    { threshold: 0.15, rootMargin: '0px 0px -40px 0px' }
  );
  fadeEls.forEach((el) => fadeObs.observe(el));

  // Relancer la cascade word-reveal après changement de langue
  document.addEventListener('i18n:applied', () => {
    const allTitres = document.querySelectorAll<HTMLElement>('.titre-words');
    allTitres.forEach((el) => {
      el.removeAttribute('data-revealed');
      splitIntoWords(el);
      const rect = el.getBoundingClientRect();
      const inView = rect.top < window.innerHeight && rect.bottom > 0;
      if (inView) {
        el.dataset.revealed = '1';
        el.querySelectorAll<HTMLElement>('.word').forEach((w, i) => {
          setTimeout(() => w.classList.add('is-revealed'), i * 70);
        });
      } else {
        titresObs.observe(el);
      }
    });
  });
</script>

<style>
/* ─── PAGE WRAPPER ─── */
.contact-page {
  position: relative;
  min-height: 100vh;
  background: var(--gradient-hero);
  overflow: hidden;
  display: flex;
  align-items: center;
  padding: 8rem 0 6rem;
}

.contact-page__bg { position: absolute; inset: 0; pointer-events: none; }
.cp-blob {
  position: absolute; border-radius: 50%;
  filter: blur(130px);
  animation: cp-float 20s ease-in-out infinite;
}
.cp-blob--1 {
  width: 700px; height: 700px;
  top: -200px; right: -150px;
  background: rgba(107,145,212,.18);
}
.cp-blob--2 {
  width: 500px; height: 500px;
  bottom: -180px; left: -100px;
  background: rgba(160,180,222,.14);
  animation-delay: -8s;
}
.cp-blob--3 {
  width: 400px; height: 400px;
  top: 40%; left: 30%;
  background: rgba(107,145,212,.08);
  animation-delay: -14s;
}
@keyframes cp-float {
  0%, 100% { transform: translate(0, 0) scale(1); }
  33%      { transform: translate(40px, -30px) scale(1.05); }
  66%      { transform: translate(-30px, 40px) scale(0.96); }
}
.cp-dots {
  position: absolute; inset: 0;
  background-image: radial-gradient(circle, rgba(160,180,222,.1) 1px, transparent 1px);
  background-size: 52px 52px;
  mask-image: radial-gradient(ellipse at center, black 0%, transparent 70%);
  -webkit-mask-image: radial-gradient(ellipse at center, black 0%, transparent 70%);
}

/* ─── INNER ─── */
.contact-inner {
  position: relative;
  max-width: 1180px;
  z-index: 2;
}

/* ─── WORD REVEAL ─── */
.word {
  display: inline-block;
  opacity: 0;
  transform: translateY(22px);
  filter: blur(8px);
  transition:
    opacity .65s cubic-bezier(.23,1,.32,1),
    transform .65s cubic-bezier(.23,1,.32,1),
    filter .65s cubic-bezier(.23,1,.32,1);
}
.word.is-revealed {
  opacity: 1;
  transform: translateY(0);
  filter: blur(0);
}

.reveal-fade {
  opacity: 0;
  transform: translateY(24px);
  transition: opacity .85s ease, transform .85s ease;
}
.reveal-fade.is-revealed {
  opacity: 1;
  transform: translateY(0);
}

/* ─── TOP ─── */
.contact-top { text-align: center; margin-bottom: 4rem; }
.contact-titre {
  font-size: clamp(2.6rem, 6vw, 5rem);
  font-weight: 800;
  color: #fff;
  letter-spacing: -.045em;
  line-height: 1.05;
  margin: .8rem 0 1rem;
}
.contact-titre__line { display: block; }
.contact-titre__line--grad,
.cp-card__titre-grad {
  background: linear-gradient(100deg,
    #6B91D4 0%, #A0B4DE 25%, #c8d8f0 50%, #A0B4DE 75%, #6B91D4 100%);
  background-size: 220% 100%;
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  color: transparent;
  animation: gradient-shift 5s ease-in-out infinite;
}
@keyframes gradient-shift {
  0%, 100% { background-position: 0% 50%; }
  50%      { background-position: 100% 50%; }
}

.contact-desc {
  font-size: 1.1rem;
  color: rgba(255,255,255,.6);
  margin: 0 auto;
}

/* ─── GRID ─── */
.contact-grid {
  display: grid;
  grid-template-columns: 0.85fr 1fr;
  gap: 2rem;
  align-items: stretch;
}

/* ─── CARTE COORDONNÉES PREMIUM ─── */
.cp-card {
  position: relative;
  padding: 2.4rem 2rem 2rem;
  border-radius: var(--rayon-xl);
  background:
    linear-gradient(155deg, rgba(160,180,222,.1), rgba(107,145,212,.04));
  border: 1px solid rgba(160,180,222,.22);
  backdrop-filter: blur(16px);
  overflow: hidden;
  display: flex;
  flex-direction: column;
}
.cp-card__halo {
  position: absolute;
  width: 360px; height: 360px;
  top: -120px; right: -120px;
  background: radial-gradient(circle, rgba(160,180,222,.25), transparent 65%);
  filter: blur(50px);
  border-radius: 50%;
  pointer-events: none;
  animation: cp-float 14s ease-in-out infinite;
}

.cp-card__head { margin-bottom: 1.8rem; position: relative; z-index: 2; }
.cp-card__eyebrow {
  display: block;
  font-size: .72rem;
  font-weight: 600;
  letter-spacing: .3em;
  text-transform: uppercase;
  color: rgba(160,180,222,.65);
  margin-bottom: .8rem;
}
.cp-card__titre {
  font-size: clamp(1.5rem, 2.8vw, 2rem);
  font-weight: 800;
  letter-spacing: -.025em;
  line-height: 1.1;
  color: #fff;
}

.cp-card__items {
  display: flex;
  flex-direction: column;
  gap: .8rem;
  position: relative;
  z-index: 2;
}

.cp-item {
  display: flex;
  align-items: center;
  gap: 1rem;
  padding: 1rem 1.1rem;
  background: rgba(255,255,255,.04);
  border: 1px solid rgba(160,180,222,.15);
  border-radius: var(--rayon);
  color: #fff;
  text-decoration: none;
  position: relative;
  overflow: hidden;
  transition: all .35s cubic-bezier(.23,1,.32,1);
}
.cp-item::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, rgba(107,145,212,.15), transparent 60%);
  opacity: 0;
  transition: opacity .35s ease;
}
.cp-item--link { cursor: pointer; }
.cp-item--link:hover {
  background: rgba(107,145,212,.1);
  border-color: rgba(107,145,212,.45);
  transform: translateX(4px);
  box-shadow: 0 12px 30px rgba(13,24,48,.35);
}
.cp-item--link:hover::before { opacity: 1; }
.cp-item--link:hover .cp-item__ico {
  transform: scale(1.08) rotate(-4deg);
  background: linear-gradient(135deg, rgba(107,145,212,.4), rgba(160,180,222,.2));
  color: #fff;
}
.cp-item--link:hover .cp-item__arrow {
  opacity: 1;
  transform: translateX(2px);
  color: var(--bleu-clair);
}

.cp-item__ico {
  width: 44px;
  height: 44px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, rgba(107,145,212,.22), rgba(160,180,222,.08));
  border: 1px solid rgba(160,180,222,.18);
  border-radius: 12px;
  color: var(--bleu);
  transition: all .4s cubic-bezier(.23,1,.32,1);
  position: relative;
  z-index: 2;
}
.cp-item__ico svg {
  width: 22px;
  height: 22px;
}
.cp-item__ico--alt {
  background: rgba(160,180,222,.08);
  color: rgba(160,180,222,.6);
}

.cp-item__txt {
  display: flex;
  flex-direction: column;
  gap: .15rem;
  flex: 1;
  position: relative;
  z-index: 2;
  min-width: 0;
}
.cp-item__label {
  font-size: .68rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: .2em;
  color: rgba(160,180,222,.55);
}
.cp-item__val {
  font-size: .98rem;
  font-weight: 600;
  color: rgba(255,255,255,.92);
  letter-spacing: -.005em;
  word-break: break-all;
}

.cp-item__arrow {
  width: 18px;
  height: 18px;
  margin-left: auto;
  color: rgba(107,145,212,.55);
  opacity: 0.6;
  transition: all .35s ease;
  flex-shrink: 0;
  z-index: 2;
}

.cp-card__separator {
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(160,180,222,.25), transparent);
  margin: 1.6rem 0 1.2rem;
  position: relative;
  z-index: 2;
}

/* Socials */
.cp-socials {
  display: flex;
  gap: .7rem;
  position: relative;
  z-index: 2;
}
.cp-social {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: .55rem;
  flex: 1;
  padding: .8rem 1rem;
  background: rgba(255,255,255,.04);
  border: 1px solid rgba(160,180,222,.12);
  border-radius: var(--rayon);
  font-size: .85rem;
  font-weight: 600;
  color: rgba(255,255,255,.7);
  text-decoration: none;
  transition: all .35s ease;
}
.cp-social:hover {
  background: rgba(107,145,212,.15);
  border-color: rgba(107,145,212,.4);
  color: #fff;
  transform: translateY(-2px);
}

/* ─── FORMULAIRE ─── */
.cp-form-wrap {
  background: rgba(255,255,255,.04);
  border: 1px solid rgba(160,180,222,.18);
  border-radius: var(--rayon-xl);
  padding: 2.4rem 2.2rem;
  backdrop-filter: blur(20px);
}

.cp-form-head { margin-bottom: 1.8rem; }

.cp-form { display: flex; flex-direction: column; gap: 1.2rem; }
.cp-form__row { display: grid; grid-template-columns: 1fr 1fr; gap: 1.2rem; }

.cp-form-wrap label {
  font-size: .8rem;
  font-weight: 700;
  color: rgba(255,255,255,.7);
  text-transform: uppercase;
  letter-spacing: .08em;
}
.cp-form-wrap input,
.cp-form-wrap textarea {
  background: rgba(255,255,255,.06);
  border: 1.5px solid rgba(160,180,222,.18);
  color: #fff;
  border-radius: var(--rayon);
  padding: .95rem 1.1rem;
  font-size: .95rem;
  font-family: inherit;
  transition: all .3s ease;
}
.cp-form-wrap input::placeholder,
.cp-form-wrap textarea::placeholder {
  color: rgba(255,255,255,.28);
}
.cp-form-wrap input:focus,
.cp-form-wrap textarea:focus {
  border-color: rgba(107,145,212,.6);
  background: rgba(107,145,212,.08);
  box-shadow: 0 0 0 3px rgba(107,145,212,.15);
  outline: none;
}

.cp-submit { width: 100%; justify-content: center; padding: 1.05rem; font-size: 1rem; }
.cp-note { font-size: .78rem; color: rgba(255,255,255,.32); text-align: center; }

/* ─── RESPONSIVE ─── */
@media (max-width: 980px) {
  .contact-grid { grid-template-columns: 1fr; }
}
@media (max-width: 600px) {
  .contact-page { padding: 7rem 0 5rem; }
  .cp-card,
  .cp-form-wrap { padding: 1.8rem 1.4rem; }
  .cp-form__row { grid-template-columns: 1fr; }
  .cp-socials { flex-direction: column; }
}

/* ═══════════════════════════════════════════
   CALENDLY HERO — Section voie rapide
   ═══════════════════════════════════════════ */
.calendly-hero {
  position: relative;
  margin: 0 0 4rem;
  border-radius: var(--rayon-xl, 3rem);
  overflow: hidden;
  background: linear-gradient(135deg, #0d1830 0%, #1a2e50 60%, #243d6b 100%);
  border: 1px solid rgba(160,180,222,.22);
  box-shadow:
    0 30px 90px -20px rgba(7,13,26,.6),
    0 0 0 1px rgba(255,255,255,.04) inset,
    0 0 60px -20px rgba(107,145,212,.35) inset;
}
.calendly-hero__bg {
  position: absolute; inset: 0; pointer-events: none; z-index: 0;
}
.calendly-hero__glow {
  position: absolute;
  top: -30%; right: -10%;
  width: 60%; height: 140%;
  background: radial-gradient(circle, rgba(160,180,222,.35) 0%, rgba(107,145,212,.15) 35%, transparent 70%);
  filter: blur(40px);
  animation: calendly-glow 8s ease-in-out infinite;
}
@keyframes calendly-glow {
  0%, 100% { transform: translate(0, 0) scale(1); opacity: .7; }
  50%      { transform: translate(-30px, 20px) scale(1.1); opacity: 1; }
}
.calendly-hero__grid {
  position: absolute; inset: 0;
  background-image:
    linear-gradient(rgba(160,180,222,.06) 1px, transparent 1px),
    linear-gradient(90deg, rgba(160,180,222,.06) 1px, transparent 1px);
  background-size: 48px 48px;
  mask-image: radial-gradient(ellipse at 70% 50%, black 0%, transparent 70%);
  -webkit-mask-image: radial-gradient(ellipse at 70% 50%, black 0%, transparent 70%);
}
.calendly-hero__content {
  position: relative; z-index: 1;
  display: grid;
  grid-template-columns: 1.4fr 1fr;
  align-items: center;
  gap: 3rem;
  padding: 3.5rem 3.5rem;
}
.calendly-hero__left {
  display: flex; flex-direction: column; gap: 1.2rem;
}
.calendly-hero__badge {
  display: inline-flex; align-items: center; gap: .55rem;
  align-self: flex-start;
  padding: .45rem .9rem;
  background: rgba(160,180,222,.12);
  border: 1px solid rgba(160,180,222,.3);
  border-radius: 999px;
  font-size: .78rem;
  font-weight: 600;
  letter-spacing: .08em;
  text-transform: uppercase;
  color: #d6e6ff;
}
.calendly-hero__badge-pulse {
  width: 8px; height: 8px;
  background: #6BE2A8;
  border-radius: 50%;
  box-shadow: 0 0 0 0 rgba(107,226,168,.7);
  animation: calendly-pulse 1.8s ease-out infinite;
}
@keyframes calendly-pulse {
  0%   { box-shadow: 0 0 0 0 rgba(107,226,168,.7); }
  70%  { box-shadow: 0 0 0 10px rgba(107,226,168,0); }
  100% { box-shadow: 0 0 0 0 rgba(107,226,168,0); }
}
.calendly-hero__titre {
  font-size: clamp(1.8rem, 3.8vw, 2.8rem);
  font-weight: 900;
  letter-spacing: -.035em;
  line-height: 1.08;
  color: #fff;
  margin: 0;
}
.calendly-hero__titre-grad {
  background: linear-gradient(120deg, #A0B4DE 0%, #d6e6ff 50%, #A0B4DE 100%);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  color: transparent;
}
.calendly-hero__desc {
  font-size: 1.02rem;
  line-height: 1.65;
  color: rgba(255,255,255,.78);
  margin: 0;
  max-width: 480px;
}
.calendly-hero__features {
  display: flex; flex-wrap: wrap; gap: .8rem 1.4rem;
  margin-top: .2rem;
}
.calendly-hero__feat {
  display: inline-flex; align-items: center; gap: .45rem;
  font-size: .9rem; color: rgba(255,255,255,.85); font-weight: 500;
}
.calendly-hero__feat svg { color: #6BE2A8; flex-shrink: 0; }
.calendly-hero__cta {
  display: inline-flex; align-items: center; gap: .7rem;
  align-self: flex-start;
  padding: 1.05rem 1.8rem;
  margin-top: .8rem;
  background: linear-gradient(135deg, #A0B4DE 0%, #6B91D4 50%, #4a70c0 100%);
  color: #0d1830;
  font-weight: 700;
  font-size: 1rem;
  letter-spacing: -.01em;
  border-radius: 999px;
  text-decoration: none;
  box-shadow:
    0 10px 30px -8px rgba(160,180,222,.5),
    0 0 0 1px rgba(255,255,255,.2) inset;
  transition: transform .35s cubic-bezier(.22,.9,.3,1), box-shadow .35s ease;
  position: relative;
  overflow: hidden;
}
.calendly-hero__cta::before {
  content: '';
  position: absolute; inset: 0;
  background: linear-gradient(120deg, transparent 30%, rgba(255,255,255,.4) 50%, transparent 70%);
  transform: translateX(-100%);
  transition: transform .8s ease;
}
.calendly-hero__cta:hover {
  transform: translateY(-3px) scale(1.02);
  box-shadow:
    0 16px 40px -8px rgba(160,180,222,.6),
    0 0 0 1px rgba(255,255,255,.3) inset;
}
.calendly-hero__cta:hover::before { transform: translateX(100%); }
.calendly-hero__cta svg:last-child { transition: transform .35s ease; }
.calendly-hero__cta:hover svg:last-child { transform: translateX(4px); }

/* Visuel mock-calendrier à droite */
.calendly-hero__right {
  display: flex; align-items: center; justify-content: center;
}
.calendly-hero__visual {
  position: relative;
  width: 100%; max-width: 320px;
  aspect-ratio: 1 / 1;
  display: flex; align-items: center; justify-content: center;
}
.calendly-hero__visual::before {
  content: '';
  position: absolute; inset: -20px;
  background: radial-gradient(circle, rgba(160,180,222,.3) 0%, transparent 60%);
  filter: blur(30px);
  z-index: 0;
}
.calendly-hero__visual-cal {
  position: relative; z-index: 1;
  width: 100%;
  background: linear-gradient(160deg, rgba(255,255,255,.08) 0%, rgba(255,255,255,.02) 100%);
  border: 1px solid rgba(255,255,255,.12);
  border-radius: 1.4rem;
  padding: 1.2rem;
  backdrop-filter: blur(10px);
  box-shadow: 0 20px 50px -10px rgba(0,0,0,.4);
  transform: rotate(-3deg);
  transition: transform .5s cubic-bezier(.22,.9,.3,1);
}
.calendly-hero:hover .calendly-hero__visual-cal { transform: rotate(0deg) scale(1.03); }
.calendly-hero__visual-head {
  display: flex; gap: .35rem; margin-bottom: 1rem;
}
.calendly-hero__visual-head span {
  width: 10px; height: 10px; border-radius: 50%;
  background: rgba(255,255,255,.25);
}
.calendly-hero__visual-grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: .35rem;
  margin-bottom: 1rem;
}
.calendly-hero__visual-grid > div {
  aspect-ratio: 1 / 1;
  background: rgba(255,255,255,.06);
  border-radius: 6px;
  transition: background .3s ease;
}
.calendly-hero__visual-grid > div.active {
  background: linear-gradient(135deg, #A0B4DE, #6B91D4);
  box-shadow: 0 0 16px rgba(160,180,222,.6);
  animation: cal-day-pulse 2s ease-in-out infinite;
}
@keyframes cal-day-pulse {
  0%, 100% { transform: scale(1); }
  50%      { transform: scale(1.15); }
}
.calendly-hero__visual-time {
  text-align: center;
  font-size: .82rem;
  color: #d6e6ff;
  font-weight: 600;
  letter-spacing: .02em;
  padding: .55rem;
  background: rgba(160,180,222,.12);
  border-radius: 8px;
  border: 1px solid rgba(160,180,222,.2);
}

/* Diviseur "ou par message" */
.contact-divider {
  display: flex; align-items: center; gap: 1.2rem;
  margin: 0 0 3rem;
  max-width: 600px;
  margin-left: auto; margin-right: auto;
}
.contact-divider__line {
  flex: 1; height: 1px;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,.18), transparent);
}
.contact-divider__text {
  font-size: .8rem;
  text-transform: uppercase;
  letter-spacing: .15em;
  color: rgba(255,255,255,.5);
  font-weight: 600;
}

@media (max-width: 900px) {
  .calendly-hero__content {
    grid-template-columns: 1fr;
    padding: 2.5rem 2rem;
    gap: 2rem;
  }
  .calendly-hero__right { order: -1; }
  .calendly-hero__visual { max-width: 220px; }
}
@media (max-width: 600px) {
  .calendly-hero { border-radius: 1.6rem; }
  .calendly-hero__content { padding: 2rem 1.4rem; gap: 1.5rem; }
  .calendly-hero__titre { font-size: clamp(1.5rem, 6.5vw, 2rem); }
  .calendly-hero__desc { font-size: .95rem; }
  .calendly-hero__cta { padding: .95rem 1.4rem; font-size: .95rem; }
  .calendly-hero__features { gap: .5rem 1rem; }
  .calendly-hero__feat { font-size: .85rem; }
  .calendly-hero__visual { max-width: 180px; }
}
</style>
